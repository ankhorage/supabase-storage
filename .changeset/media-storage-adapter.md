---
'@ankhorage/supabase-storage': minor
---

Add Supabase object listing and signed URL operations plus an optional `@ankhorage/contracts` bridge that implements the canonical `MediaStorageAdapter` for app-authoring media.
